async function buildResult(gameInfo) {
  await console.log(gameInfo);
  // const infoArray = [gameInfo.gamePk, gameInfo.gameData.datetime.dateTime];
  // infoArray.push(gameInfo.gameData.teams.away.name);
  // infoArray.push(gameInfo.liveData.linescore.teams.away.goals);
  // infoArray.push(gameInfo.gameData.teams.home.name);
  // infoArray.push(gameInfo.liveData.linescore.teams.home.goals);
  // infoArray.push(gameInfo.gameData.venue.name);
  // return infoArray;

}

export default buildResult